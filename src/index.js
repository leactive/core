/* @flow */
import createStore from './createStore'
import bindActions from './bindActions'
import composeMiddlewares from './composeMiddlewares'

export { createStore, bindActions, composeMiddlewares }

export default {
  createStore,
  bindActions,
  composeMiddlewares,
}
